{
  "name": "hpe-ltt-password-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HPE Library & Tape Tools support password generator for LTO tape drive maintenance",
  "license": "MIT",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=20" }
}
